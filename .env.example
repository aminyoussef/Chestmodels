# =============================================================================
# Medical Scans — AI Docker stack template
# Copy to .env:  cp .env.example .env
# Use the SAME API keys as the PHP app .env on Hostinger.
# =============================================================================

EEG_SERVICE_API_KEY=change-me-eeg-secret
CHEST_SERVICE_API_KEY=change-me-chest-secret

EEG_HOST_PORT=8000
CHEST_HOST_PORT=5000
THRESHOLD=0.5
